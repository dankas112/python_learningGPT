Исключения в Python (основа, но очень практично)
📌 Что такое исключение?

Это ошибка, которая прерывает программу, если её не обработать.

Например:

print(10 / 0)


Бах — ZeroDivisionError.

Или:

int("abc")


Бах — ValueError.

Python предлагает обрабатывать исключения, чтобы программа не падала.

✨ Конструкция try/except

Базовый вариант:

try:
    опасный_код
except:
    что_делать_при_ошибке


Пример:

try:
    x = int(input("Введите число: "))
    print(10 / x)
except:
    print("Произошла ошибка")

🎯 Важно: всегда указывай тип ошибки!
try:
    x = int("abc")
except ValueError:
    print("Вы ввели не число")

✨ Несколько except
try:
    x = int(input("Введите число: "))
    print(10 / x)
except ValueError:
    print("Это не число")
except ZeroDivisionError:
    print("Делить на ноль нельзя!")

🔥 Блоки else и finally
else — если ошибок не было
try:
    x = int(input())
except ValueError:
    print("Ошибка")
else:
    print("Все отлично")

finally — выполняется всегда

(Полезно для закрытия файлов, соединений, очистки ресурсов)

try:
    file = open("data.txt")
    print(file.read())
except FileNotFoundError:
    print("Файл не найден")
finally:
    print("Закрываю файл")

⚠️ Создание своих исключений
def check_age(age):
    if age < 0:
        raise ValueError("Возраст не может быть отрицательным")
