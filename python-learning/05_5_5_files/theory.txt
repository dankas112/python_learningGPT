📁 Файлы в Python
1️⃣ Открытие файла

Функция open() создаёт объект файла:

f = open("example.txt", "r", encoding="utf-8")


"r" — режим чтения (read)

"w" — режим записи (write, перезаписывает файл)

"a" — режим добавления (append)

"x" — создать новый файл (ошибка, если уже существует)

"b" — бинарный режим (для изображений, видео)

"t" — текстовый режим (по умолчанию)

encoding="utf-8" — чтобы корректно работал русский текст

2️⃣ Чтение файла
a) Весь текст целиком
with open("example.txt", "r", encoding="utf-8") as f:
    content = f.read()
print(content)


Обратите внимание на with — это контекстный менеджер, который сам закроет файл, даже если произойдёт ошибка.

b) По строкам
with open("example.txt", "r", encoding="utf-8") as f:
    for line in f:
        print(line.strip())  # strip убирает перенос строки

c) Список строк
with open("example.txt", "r", encoding="utf-8") as f:
    lines = f.readlines()  # вернёт список строк
print(lines)

3️⃣ Запись в файл
a) Перезапись
with open("example.txt", "w", encoding="utf-8") as f:
    f.write("Привет, мир!\n")
    f.write("Ещё одна строка")


Режим "w" перезаписывает файл.

b) Добавление
with open("example.txt", "a", encoding="utf-8") as f:
    f.write("\nДобавленная строка")

4️⃣ Работа с исключениями
try:
    with open("not_exist.txt", "r", encoding="utf-8") as f:
        print(f.read())
except FileNotFoundError:
    print("Файл не найден")
except Exception as e:
    print("Произошла ошибка:", e)


Так можно безопасно работать с файлами и ловить ошибки.

5️⃣ Полезные методы файлов

f.read(size) — прочитать size символов

f.readline() — прочитать одну строку

f.readlines() — прочитать все строки в список

f.write(text) — записать текст

f.writelines(list_of_lines) — записать список строк

6️⃣ Практическая задача (мини)

Создать файл tasks.txt.

Записать туда 3 задачи (по одной строке).

Прочитать файл и посчитать, сколько задач.

Добавить ещё одну задачу.