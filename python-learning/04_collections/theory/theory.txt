Блок collections

collections — это встроенный модуль Python, который даёт более продвинутые и удобные структуры данных, чем стандартные списки, словари и кортежи.

Основные элементы:

namedtuple

Кортеж с именованными полями (удобно для хранения “структурированных” данных)

from collections import namedtuple

Point = namedtuple('Point', ['x', 'y'])
p = Point(10, 20)
print(p.x, p.y)  # 10 20


deque

Двусторонняя очередь (очень быстро добавлять/удалять элементы с начала и конца)

from collections import deque

dq = deque([1,2,3])
dq.appendleft(0)
dq.append(4)
print(dq)  # deque([0, 1, 2, 3, 4])


Counter

Считает количество одинаковых элементов

from collections import Counter

c = Counter(['a','b','a','c','b','a'])
print(c)  # Counter({'a': 3, 'b': 2, 'c': 1})


OrderedDict (в Python 3.7+ обычный dict уже сохраняет порядок, но полезно знать)

Словарь, который помнит порядок добавления элементов

defaultdict

Словарь с дефолтным значением, чтобы не проверять ключи на существование

from collections import defaultdict

dd = defaultdict(int)
dd['a'] += 1
dd['b'] += 2
print(dd)  # defaultdict(<class 'int'>, {'a': 1, 'b': 2})


💡 Идея изучения:

collections — это как “улучшенные списки и словари”.

Очень полезно при подсчётах, очередях, работе с текстом, обработке данных.

После этого блока можно легче решать более сложные алгоритмические задачи.