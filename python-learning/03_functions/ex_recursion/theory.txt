Рекурсия — это когда функция вызывает саму себя, чтобы решить задачу по шагам.
Главное — чтобы у рекурсии всегда было условие выхода, иначе программа уйдёт в бесконечный цикл вызовов.

🔹 Простейший пример: обратный отсчёт
def countdown(n):
    if n == 0:
        print("Пуск!")
    else:
        print(n)
        countdown(n - 1)

countdown(5)


🔸 Как это работает:

countdown(5) печатает 5 и вызывает countdown(4)

затем 4, 3, 2, 1 — и наконец 0 → “Пуск!”

🔹 Факториал через рекурсию
def factorial(n):
    if n == 1:
        return 1
    else:
        return n * factorial(n - 1)

print(factorial(5))


Пошагово:

factorial(5)
= 5 * factorial(4)
= 5 * 4 * factorial(3)
= 5 * 4 * 3 * factorial(2)
= 5 * 4 * 3 * 2 * factorial(1)
= 5 * 4 * 3 * 2 * 1
= 120

🔹 Сумма чисел от 1 до n
def sum_to_n(n):
    if n == 0:
        return 0
    else:
        return n + sum_to_n(n - 1)

print(sum_to_n(10))

🔹 Числа Фибоначчи (через рекурсию)
def fib(n):
    if n <= 1:
        return n
    return fib(n - 1) + fib(n - 2)

print(fib(7))  # 13


(но помни — такая реализация неэффективна, потому что вызывает себя дважды)