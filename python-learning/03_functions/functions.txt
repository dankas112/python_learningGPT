Что такое функция

Функция — это блок кода, который выполняет определённую задачу и может быть вызван в любом месте программы.
Она помогает избежать дублирования кода и делает программы структурированнее.

📘 Объявление функции
def имя_функции(параметры):
    # тело функции
    инструкция_1
    инструкция_2
    return результат


Пример:

def greet():
    print("Привет, мир!")

greet()

⚙️ Аргументы функции
1. Без аргументов
def say_hello():
    print("Привет!")

say_hello()

2. С аргументами
def greet(name):
    print(f"Привет, {name}!")

greet("Артём")

3. Несколько аргументов
def add(a, b):
    print(a + b)

add(5, 3)

🎯 Возврат значения — return

Если функция должна вернуть результат, используется return:

def add(a, b):
    return a + b

result = add(3, 7)
print(result)

🧮 Аргументы по умолчанию

Можно указать значение, которое используется, если аргумент не передан:

def greet(name="друг"):
    print(f"Привет, {name}!")

greet()          # выведет: Привет, друг!
greet("Аня")     # выведет: Привет, Аня!

🔀 Именованные аргументы

Аргументы можно передавать в любом порядке, если указать их имена:

def info(name, age):
    print(f"{name}, {age} лет")

info(age=25, name="Даня")

🌐 Локальные и глобальные переменные

Локальные — создаются внутри функции и существуют только там.

Глобальные — доступны везде в программе.

x = 10  # глобальная

def test():
    x = 5  # локальная
    print(x)

test()
print(x)

⚡ Lambda-функции

Анонимные функции, короткие и простые:

square = lambda x: x ** 2
print(square(5))


Эквивалент:

def square(x):
    return x ** 2