Типы данных в Python — расширенная база
1️⃣ Логический тип — bool

Хранит True или False.

Результат логических операций и проверок.

Пустые значения считаются False: 0, '', [], {}, None.

Примеры:

a = 5
b = 10
print(a < b)        # True
print(bool(0))      # False
print(bool("Hi"))   # True

2️⃣ Кортеж — tuple

Неизменяемый список.

Создаётся в круглых скобках ( ).

Можно индексировать и перебирать.

Примеры:

my_tuple = (1, 2, 3)
print(my_tuple[0])   # 1
print(len(my_tuple)) # 3

3️⃣ Множество — set

Набор уникальных элементов, порядок не гарантируется.

Создаётся через {} или set().

Поддерживает операции: | (объединение), & (пересечение), - (разность).

Примеры:

a = {1, 2, 3, 3}
b = {3, 4, 5}
print(a)     # {1, 2, 3}
print(a | b) # {1, 2, 3, 4, 5}
print(a & b) # {3}

4️⃣ Словарь — dict

Хранит пары ключ → значение.

Создаётся {ключ: значение, ...}.

Доступ по ключу: dict[key].

Методы: .keys(), .values(), .items().

Примеры:

person = {"name": "Danya", "age": 22}
print(person["name"])  # Danya
person["age"] = 23
person["job"] = "student"

5️⃣ NoneType — None

Специальное значение, которое означает “ничего”.

Используется для отсутствия данных.

result = None
if result is None:
    print("Нет данных")

6️⃣ Комплексные числа — complex

Числа вида a + bj.

Используются в математике и инженерии.

Свойства: .real и .imag.

z = 2 + 3j
print(z.real)  # 2.0
print(z.imag)  # 3.0

🔹 Краткая памятка
| Тип       | Изменяемость | Пример          | Особенности                        |
| --------- | ------------ | --------------- | ---------------------------------- |
| `bool`    | нет          | `True`, `False` | Результат условий                  |
| `tuple`   | нет          | `(1, 2, 3)`     | Индексация, неизменяемость         |
| `set`     | да           | `{1,2,3}`       | Уникальные элементы, быстрый поиск |
| `dict`    | да           | `{"a":1}`       | Ключ → значение                    |
| `None`    | нет          | `None`          | Отсутствие данных                  |
| `complex` | да           | `2+3j`          | Реальная и мнимая часть            |
