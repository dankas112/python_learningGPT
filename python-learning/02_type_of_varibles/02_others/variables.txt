Типы данных в Python (дополнение)
🔹 1. Логический тип (bool)

Хранит одно из двух значений:

True
False


— результат логических операций и сравнений.

Примеры:

a = 5
b = 10
print(a < b)      # True
print(bool(0))    # False
print(bool("Hi")) # True (любая непустая строка = True)


Используется в условиях (if, while и т. д.).

🔹 2. Кортеж (tuple)

Похож на список, но неизменяемый.
Создаётся с помощью круглых скобок:

my_tuple = (1, 2, 3)


Особенности:

нельзя изменять элементы (append, remove — недоступны),

можно перебирать циклом и индексировать.

print(my_tuple[1])      # 2
print(len(my_tuple))    # 3


Часто используется для хранения постоянных наборов данных.

🔹 3. Множество (set)

Набор уникальных элементов (без повторов, порядок не гарантируется):

my_set = {1, 2, 3, 3, 2}
print(my_set)  # {1, 2, 3}


Операции с множествами:

a = {1, 2, 3}
b = {3, 4, 5}
print(a | b)   # объединение {1, 2, 3, 4, 5}
print(a & b)   # пересечение {3}
print(a - b)   # разность {1, 2}


Используется для фильтрации повторов и быстрой проверки принадлежности.

🔹 4. Словарь (dict)

Хранит пары ключ–значение.

person = {
    "name": "Артем",
    "age": 25,
    "city": "Москва"
}


Доступ по ключу:

print(person["name"])      # Артем


Изменение:

person["age"] = 26
person["job"] = "инженер"


Методы:

person.keys()
person.values()
person.items()

🔹 5. NoneType

Специальное значение — “ничего”, “пусто”.
Используется для обозначения отсутствия данных.

result = None
if result is None:
    print("Нет данных")

🔹 6. Комплексные числа (complex)

Используются редко (в математике, инженерных расчётах).

z = 2 + 3j
print(z.real)  # 2.0
print(z.imag)  # 3.0