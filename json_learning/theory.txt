1. Теория: что такое JSON
✔ Что такое JSON

JSON (JavaScript Object Notation) — это формат хранения данных в виде текста.
Он используется почти везде: API, конфигурации, сохранение настроек, обмен между программами.

По сути это:

словари → { ... }

списки → [ ... ]

строки → "text"

числа → 123

логические → true/false

null → null

В Python это напрямую соответствует:

dict

list

str

int/float

True/False

None

✔ Основные преимущества

Легко читается человеком

Легко парсится программами

Во всех языках одинаковый формат

Универсальный способ обмена данными

✔ Ограничения JSON

Комментарии запрещены

Только двойные кавычки (", не ')

Ключи объектов — только строки

Нельзя хранить Python-объекты (классы, функции, datetime…)

Нет tuple → превращается в list

🧰 2. Как работать с JSON в Python

В Python работа с JSON делается через стандартный модуль:

import json

⭐ Основные функции:
Функция	Что делает
json.loads(str)	Превращает JSON-строку в Python объект
json.load(file)	Прочитать JSON из файла
json.dumps(obj)	Превратить Python объект → JSON строку
json.dump(obj, file)	Записать Python объект в файл в JSON
📥 3. Чтение JSON
Пример JSON-файла:

data.json:

{
    "name": "Danila",
    "age": 19,
    "languages": ["Python", "JavaScript"]
}

Как прочитать:
import json

with open("data.json", "r", encoding="utf-8") as f:
    data = json.load(f)

print(data["name"])        # Danila
print(data["languages"])   # ['Python', 'JavaScript']

📤 4. Запись JSON
person = {
    "name": "Danila",
    "age": 19,
    "skills": ["python", "sql"]
}

import json

with open("person.json", "w", encoding="utf-8") as f:
    json.dump(person, f, ensure_ascii=False, indent=4)


Параметры:

ensure_ascii=False → оставляет кириллицу

indent=4 → делает красивый формат

🧂 5. Частые мелкие моменты / особенности
✔ 1. Не путай load и loads
Что есть	Пример
load — из файла	json.load(f)
loads — из строки	json.loads('{"a":1}')
✔ 2. При записи всегда делай indent=

Так файл readable.

✔ 3. Проверяй, что файл существует

Иначе будет ошибка FileNotFoundError.

✔ 4. Работай с try-except, потому что JSON может быть сломан
try:
    data = json.load(f)
except json.JSONDecodeError:
    print("Файл повреждён")

✔ 5. После чтения ты работаешь с обычными dict/list

Типы те же самые.

✔ 6. JSON не хранит tuple → станет list